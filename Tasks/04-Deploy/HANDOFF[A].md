# Handoff into Phase 04

Stub. Rewritten for real when Phase 03 closes.

Target is settled: GitHub Pages, custom domain `shevinum.dev`, no base path.

Phase 03 is expected to settle:
- `astro.config.mjs` final state — `site: 'https://shevinum.dev'`, `base` deleted.
- Whether the build emits any link still carrying the old `/academic-portfolio-astro/` base.
- Whether `public/robots.txt` and the sitemap point at `shevinum.dev`.
- The README's final shape, so the Deploying section slots into it.
