# Phase 02b — Switch to pnpm

Status: PLANNED. Blocked on Phase 02.

Added mid-run at your request.

Next: delete `package-lock.json`, install with pnpm, pin `pnpm@11.20.0` in `package.json`, and update the commands in the README and AGENTS.md. Same dependency versions — this is a tool swap, not an upgrade.
