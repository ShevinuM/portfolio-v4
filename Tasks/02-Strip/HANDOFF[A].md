# Handoff into Phase 02

Stub. Rewritten for real when Phase 01 closes.

Phase 01 is expected to settle:
- v4 is a git repository on `main` with one baseline commit and no remote.
- The full template tree is present; the upstream `.git` is not.
- `npm install` and `npm run build` both exit 0 on the untouched template.
- The exact template source commit sha, recorded in the baseline commit message.
- Whether `.gitignore` needed any additions.
