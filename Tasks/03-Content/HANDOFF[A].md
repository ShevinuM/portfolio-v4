# Handoff into Phase 03

Stub. Rewritten for real when Phase 02 closes.

Phase 02 is expected to settle:
- The full grep enumeration of `teaching` / `dev-tools` / `Settings` references, and how each was resolved.
- Which shared files (`adapters.ts`, `tags.ts`, `rss.xml.ts`, `tags/*.astro`) needed edits when the `teaching` collection was removed.
- Whether an empty `posts` / `publications` / `projects` / `talks` collection broke any listing page, and how that was handled. `talks` stays empty for good, so its listing must tolerate zero entries permanently.
- The final footer markup after the `©` removal.
- That `src/content/bio.md` and `src/content/cv.md` are still present with Shannon content, waiting to be rewritten.
- Which Shannon assets remain in `public/`.
